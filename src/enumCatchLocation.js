/** @type { 'server' } */
const server = 'server'

/** @type { 'pageServer' } */
const pageServer = 'pageServer'

/** @typedef { server | pageServer } enumCatchLocation */

export const enumCatchLocation = { server, pageServer }
